inputs:
    dependencies:
        default: null
        required: false
runs:
    using: 'composite'
    steps:
        # - uses: actions/checkout@v4
        #   with:
        #       fetch-depth: 0
        # - uses: actions/setup-node@v4
        #   with:
        #       node-version: 'latest'
        #       cache: 'npm'
        #       # cache-dependency-path: ./${{ env.THEME_DIR }}/package-lock.json
        # - run: echo ${{ env.THEME_DIR }} '---';
        # - run: npm ci
        #   name: npm install

        # - run: npm run build
        #   name: npm build
        - run: echo ${{ inputs.dependencies }}
        - run: ls -la
