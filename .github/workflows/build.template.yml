on:
    workflow_call:

jobs:
    build:
        # name: Unit test
        # runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
