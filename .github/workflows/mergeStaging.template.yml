name: Merge to `staging`
run-name: ${{ github.repository }} - Merge to `staging` - ${{ github.run_id }}

# on:
# [push]
# workflow_run:
#     workflows: ['Production']
#     types: [completed]
#     branches:
#         - 'main'
# push:
#     branches:
#         - main

permissions: write-all

# env:
#     THEME_DIR: './wp-content/themes/demo-theme'
# defaults:
#     run:
#         working-directory: ./wp-content/themes/demo-theme

jobs:
    # call-StagingPR:
    #     uses: ./.github/workflows/stagingPR.template.yml
    BackupSite:
        name: Create backup point
        runs-on: ubuntu-latest
        steps:
            - run: echo 'Create & merge PR to `staging`'
