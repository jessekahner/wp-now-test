runs:
    using: 'composite'
    steps:
        - uses: actions/setup-node@v4
          with:
              node-version: 'latest'
              cache: 'npm'
              cache-dependency-path: ${{ env.THEME_DIR }}/package-lock.json
        # - run: npm ci
        #   name: npm install

        # - run: npm run build
        #   name: npm build
